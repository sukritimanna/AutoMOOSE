# validation/ — reproducibility evidence for the manuscript

- `gg_kinetics.py` — grain-growth kinetics pipeline (Arrhenius, finite-size, parity).
- `notebooks/`     — one notebook per figure, each reading the gg_kinetics CSV outputs.
- `data/`          — CSV outputs (or pointers to the HPC paths they came from).

Drop the files we prepared here:
  gg_kinetics.py, 01_finite_size_Q.ipynb, 02_arrhenius.ipynb, 03_parity.ipynb
